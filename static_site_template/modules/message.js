'use strict';

angular.module('staticyahoo.message', ['staticyahoo.index'])

  .factory('MessageData', function (LocalJSONP) {

    return {};

  })

  .controller('MessageCtrl', function () {
/*    {% set headers = [
      ("From:", get_display_name(message, include_email=True)),
      ("Date:", get_formatted_date(message)),
      ("Subject:", message.subject),
    ]
    %}*/

  })

;
